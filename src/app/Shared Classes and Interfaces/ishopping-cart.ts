import { IShoppingCartItem } from './ishopping-cart-item';

export class IShoppingCart {
    items: IShoppingCartItem[];

    // constructor(private items: IShoppingCartItems[]){}
    // getTotalItemsCount() {
    //     let counter = 0;
    //     for (let product in this.items) {
    //         counter += this.items[product].quantity;

    //     }
    // }
}
